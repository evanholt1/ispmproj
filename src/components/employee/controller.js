const Employee = require('./model');

module.exports = employeeController = {

    // choose employees randomly

    // send notification for system(employees) to get ready

    // ?? start appointment
}